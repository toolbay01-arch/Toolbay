# Web Push Architecture - Visual Guide

## 🎯 The Complete Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER'S DEVICE                           │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │              Browser (Can be CLOSED)                      │ │
│  │                                                           │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │  Service Worker (Always Running in Background)      │ │ │
│  │  │  - Listens for push events                          │ │ │
│  │  │  - Shows notifications                               │ │ │
│  │  │  - Handles clicks                                    │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                          ↑                                │ │
│  │                          │ Push Event                     │ │
│  └───────────────────────────────────────────────────────────┘ │
│                             ↑                                   │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                              │ 3. Deliver Push
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                             ↓                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │       Push Service (Google FCM / Mozilla / Apple)       │   │
│  │       - Routes notifications to correct device          │   │
│  │       - Handles delivery & retries                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                             ↑                                   │
│                             │ 2. Send Push Message              │
│                             │    (with VAPID auth)              │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                             ↓                                   │
│                    YOUR NEXT.JS SERVER                          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. Event Happens (Payment, Order, Message)            │   │
│  │                                                         │   │
│  │  → Check: Who should be notified?                      │   │
│  │  → Fetch: User's push subscriptions from database      │   │
│  │  → Send: Push message via web-push library             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Database (MongoDB via PayloadCMS)                      │   │
│  │                                                         │   │
│  │  PushSubscriptions Collection:                          │   │
│  │  ┌────────────────────────────────────────────────┐    │   │
│  │  │ User: leo@example.com                          │    │   │
│  │  │ Endpoint: https://fcm.googleapis.com/...       │    │   │
│  │  │ Keys: { p256dh: "...", auth: "..." }           │    │   │
│  │  │ UserAgent: Chrome/120.0 (Windows)              │    │   │
│  │  └────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Environment Variables:                                         │
│  NEXT_PUBLIC_VAPID_PUBLIC_KEY=BN3xU...                          │
│  VAPID_PRIVATE_KEY=T7g2Kp...                                    │
│  VAPID_EMAIL=mailto:admin@toolboxx.com                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Subscription Flow (One-Time Setup)

```
User Opens Website
       ↓
┌─────────────────────────────────────────┐
│ 1. Request Notification Permission      │
│    "Allow ToolBoxx to send notifications?"│
│    [Block] [Allow] ← User clicks Allow  │
└─────────────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ 2. Register Service Worker              │
│    navigator.serviceWorker.register()   │
│    → Browser installs /sw.js            │
└─────────────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ 3. Subscribe to Push Manager            │
│    pushManager.subscribe({              │
│      userVisibleOnly: true,             │
│      applicationServerKey: VAPID_KEY    │
│    })                                   │
└─────────────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ 4. Browser Contacts Push Service        │
│    → Google FCM / Mozilla / Apple       │
│    → Gets unique endpoint URL           │
│    → Generates encryption keys          │
└─────────────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ 5. Send Subscription to Your Server     │
│    POST /api/push/subscribe             │
│    {                                    │
│      endpoint: "https://fcm...",        │
│      keys: { p256dh: "...", auth: "..." }│
│    }                                    │
└─────────────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ 6. Save to Database                     │
│    payload.create({                     │
│      collection: 'push-subscriptions',  │
│      data: { user, endpoint, keys }     │
│    })                                   │
└─────────────────────────────────────────┘
       ↓
   READY! 🎉
   User can now receive push notifications
```

---

## 📨 Sending a Push Notification

```
Payment Received Event
       ↓
┌──────────────────────────────────────────┐
│ 1. Your Backend Code                     │
│    const userId = transaction.buyer.id   │
│    sendPushNotification(userId, {        │
│      title: "Payment Received",          │
│      message: "RWF 50,000",              │
│      url: "/verify-payments"             │
│    })                                    │
└──────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│ 2. Fetch User's Subscriptions            │
│    const subs = await db.find({          │
│      collection: 'push-subscriptions',   │
│      where: { user: userId }             │
│    })                                    │
└──────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│ 3. Send via web-push Library             │
│    import webPush from 'web-push'        │
│                                          │
│    webPush.setVapidDetails(              │
│      VAPID_EMAIL,                        │
│      VAPID_PUBLIC_KEY,                   │
│      VAPID_PRIVATE_KEY                   │
│    )                                     │
│                                          │
│    webPush.sendNotification(             │
│      subscription,                       │
│      JSON.stringify({                    │
│        title: "Payment Received",        │
│        body: "RWF 50,000"                │
│      })                                  │
│    )                                     │
└──────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│ 4. Google FCM (or Mozilla/Apple)         │
│    - Authenticates via VAPID             │
│    - Encrypts payload                    │
│    - Routes to user's device             │
└──────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│ 5. User's Service Worker                 │
│    self.addEventListener('push', (e) => {│
│      const data = e.data.json()          │
│      self.registration.showNotification( │
│        data.title,                       │
│        { body: data.body }               │
│      )                                   │
│    })                                    │
└──────────────────────────────────────────┘
       ↓
   🔔 NOTIFICATION APPEARS!
   (Even if browser was closed)
```

---

## 🔑 VAPID Keys Explained

```
┌────────────────────────────────────────────────────────────┐
│                    VAPID = Voluntary Application            │
│                    Server Identification for Web Push       │
└────────────────────────────────────────────────────────────┘

Purpose: Prove to push service that YOU are authorized to send
        push notifications to users who subscribed to YOUR app

┌─────────────────────────────────────────────────────────────┐
│  Generate Once (Using web-push):                            │
│                                                             │
│  npx web-push generate-vapid-keys                           │
│                                                             │
│  Output:                                                    │
│  ===============================================             │
│  Public Key:                                                │
│  BN3xU4T...  ← Share with browser (client-side)             │
│                                                             │
│  Private Key:                                               │
│  T7g2Kp...   ← Keep SECRET on server only!                  │
│  ===============================================             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  How It Works:                                              │
│                                                             │
│  1. Browser subscribes using PUBLIC key                     │
│     → Tells push service: "I want notifications from app    │
│       with public key BN3xU4T..."                           │
│                                                             │
│  2. Your server sends push using PRIVATE key                │
│     → Signs request: "I am the owner of BN3xU4T... (proves  │
│       it by signing with matching private key)"             │
│                                                             │
│  3. Push service verifies signature                         │
│     → Checks: Does private key match public key?            │
│     → If YES: Delivers notification ✅                      │
│     → If NO: Rejects (prevents spam) ❌                     │
└─────────────────────────────────────────────────────────────┘

Security Benefits:
✅ Prevents other apps from sending push to your users
✅ Proves authenticity of push sender
✅ No API keys needed for push services (Google, Mozilla, Apple)
```

---

## 🗄️ Database Storage

```
┌────────────────────────────────────────────────────────────┐
│  PushSubscriptions Collection                              │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Document 1:                                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ _id: "67abc..."                                      │ │
│  │ user: "user-123"                                     │ │
│  │ endpoint: "https://fcm.googleapis.com/fcm/send/..."  │ │
│  │ keys: {                                              │ │
│  │   p256dh: "BGt4G...",  ← Encryption key             │ │
│  │   auth: "3yKpx..."      ← Authentication secret      │ │
│  │ }                                                    │ │
│  │ userAgent: "Chrome/120.0.0.0 (Windows NT 10.0)"     │ │
│  │ isActive: true                                       │ │
│  │ createdAt: "2025-12-12T10:30:00Z"                   │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  Document 2: (Same user, different device)                 │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ _id: "68def..."                                      │ │
│  │ user: "user-123"  ← Same user                       │ │
│  │ endpoint: "https://web.push.apple.com/..."           │ │
│  │ keys: { p256dh: "...", auth: "..." }                │ │
│  │ userAgent: "Safari/17.0 (iPhone; iOS 17.2)"         │ │
│  │ isActive: true                                       │ │
│  │ createdAt: "2025-12-12T15:45:00Z"                   │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  → One user can have MULTIPLE subscriptions               │
│    (desktop browser, mobile browser, tablet, etc.)         │
│                                                            │
│  → When sending push: Send to ALL active subscriptions     │
│    for that user                                           │
└────────────────────────────────────────────────────────────┘

Storage per subscription: ~200-300 bytes
For 10,000 users: ~2-3 MB total
```

---

## 🌐 Push Services by Browser

```
┌─────────────────────────────────────────────────────────────┐
│  Chrome / Edge / Opera                                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Push Service: Google Firebase Cloud Messaging (FCM)  │ │
│  │  Endpoint: https://fcm.googleapis.com/...              │ │
│  │  Free Tier: Unlimited                                  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Firefox                                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Push Service: Mozilla Push Service                    │ │
│  │  Endpoint: https://updates.push.services.mozilla.com/  │ │
│  │  Free Tier: Unlimited                                  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Safari (macOS / iOS 16.4+)                                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Push Service: Apple Push Notification Service (APNs)  │ │
│  │  Endpoint: https://web.push.apple.com/...              │ │
│  │  Free Tier: Unlimited                                  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

✨ Good News: The 'web-push' library handles ALL of these
   automatically! You don't need to worry about which service
   to use - it's determined by the endpoint URL.
```

---

## 📂 File Structure Overview

```
toolboxx/
├── public/
│   ├── sw.js                          ← Service worker (background script)
│   ├── manifest.json                  ← PWA manifest
│   └── icons/
│       ├── icon-192x192.png          ← App icon (small)
│       └── icon-512x512.png          ← App icon (large)
│
├── src/
│   ├── collections/
│   │   └── PushSubscriptions.ts       ← Database schema
│   │
│   ├── app/
│   │   ├── layout.tsx                 ← Add manifest link here
│   │   └── api/
│   │       └── push/
│   │           ├── subscribe/
│   │           │   └── route.ts       ← Save subscription
│   │           └── send/
│   │               └── route.ts       ← Send push
│   │
│   └── lib/
│       └── notifications/
│           └── web-push.ts            ← Client-side push service
│
├── .env.local
│   NEXT_PUBLIC_VAPID_PUBLIC_KEY=...   ← Public VAPID key
│   VAPID_PRIVATE_KEY=...              ← Private VAPID key (SECRET!)
│   VAPID_EMAIL=mailto:...             ← Your email
│
└── next.config.mjs                    ← Add PWA config
```

---

## ⏱️ Timeline: From Zero to Push

```
Day 1 (3-4 hours):
├─ 00:00 - Install dependencies (web-push, next-pwa)
├─ 00:05 - Generate VAPID keys
├─ 00:10 - Create service worker (/public/sw.js)
├─ 00:40 - Create PWA manifest
├─ 00:50 - Create app icons
├─ 01:00 - Create PushSubscriptions collection
├─ 01:15 - Add collection to payload.config.ts
├─ 01:20 - Create /api/push/subscribe route
├─ 01:40 - Create /api/push/send route
├─ 02:00 - Create web-push client service
├─ 02:20 - Update next.config.mjs
├─ 02:25 - Update layout.tsx
├─ 02:30 - Create subscribe UI component
├─ 03:00 - Integrate with payment event
├─ 03:15 - Test subscription flow
├─ 03:30 - Test sending push
└─ 04:00 - Deploy & verify ✅

Then: Monitor, gather feedback, iterate!
```

---

## 🎓 Understanding Check

After reading this, you should understand:

✅ Push works even when browser is closed (service worker)  
✅ VAPID keys authenticate your server  
✅ Push service (FCM, Mozilla, Apple) routes notifications  
✅ Subscriptions are stored in your database  
✅ One user can have multiple subscriptions (multiple devices)  
✅ HTTPS is required  
✅ Setup takes 3-4 hours  
✅ Operating cost is minimal (~$0.15/month per 1000 users)

Still confused? Re-read the diagrams or check `WEB_PUSH_GUIDE.md`!
