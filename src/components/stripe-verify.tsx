import { Button, Link } from "@payloadcms/ui";

export const StripeVerify = () => {
  return (
    <Link href="/verify-tenants">
      <Button>
        Verify Tenants
      </Button>
    </Link>
  );
};
