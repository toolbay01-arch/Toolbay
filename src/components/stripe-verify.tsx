import { Button, Link } from "@payloadcms/ui";

export const StripeVerify = () => {
  return (
    <Link href="/admin/collections/tenants">
      <Button>
        Manage Tenants
      </Button>
    </Link>
  );
};
